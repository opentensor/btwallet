from bittensor_wallet.bittensor_wallet import (
    Config,
    Keyfile,
    Keypair,
    Wallet,
    config,
    errors,
    keyfile,
    keypair,
    utils,
    wallet,
    __version__,
)

# classes
Config = Config
Keyfile = Keyfile
Keypair = Keypair
Wallet = Wallet

# modules
config = config
errors = errors
keyfile = keyfile
keypair = keypair
utils = utils
wallet = wallet
__version__ == __version__
