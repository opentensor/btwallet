from bittensor_wallet.bittensor_wallet import (
    # classes
    Config as Config,
    Keyfile as Keyfile,
    Keypair as Keypair,
    Wallet as Wallet,
    # modules
    config as config,
    errors as errors,
    keyfile as keyfile,
    keypair as keypair,
    utils as utils,
    wallet as wallet,
)

__version__ = "2.0.0"
